@page
@using gpconnect_appointment_checker.Helpers.Constants
@model AccessDeniedModel
@addTagHelper *, gpconnect-appointment-checker

<div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-two-thirds">
        <div class="nhsuk-warning-callout">
            <h3 class="nhsuk-warning-callout__label">@SearchConstants.ACCESSLABEL</h3>
            <p>@Html.Raw(string.Format(SearchConstants.ACCESSTEXT, Model.GetAccessEmailAddress))</p>
            <p>@Html.Raw(string.Format(SearchConstants.ACCESSGRANTEDTEXT, "/Search"))</p>
        </div>
    </div>
</div>